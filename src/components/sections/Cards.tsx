import Image from 'next/image';import Link from 'next/link';import { Star } from 'lucide-react';import type { PortfolioItem, Service, Testimonial } from '@/types/site';
export function ServiceCard({service}:{service:Service}){const Icon=service.icon;return <Link href={`/servicos/${service.slug}`} className="focus-ring group rounded-3xl border border-slate-200 bg-white p-6 shadow-sm transition hover:-translate-y-1 hover:shadow-lg"><Icon className="mb-4 text-amber-600"/><h3 className="text-xl font-black text-slate-900">{service.title}</h3><p className="mt-2 text-slate-600">{service.summary}</p><span className="mt-4 inline-block font-bold text-slate-900">Saiba mais →</span></Link>}
export function PortfolioCard({item}:{item:PortfolioItem}){return <article className="overflow-hidden rounded-3xl bg-white shadow-sm"><Image src={item.image} alt={item.alt} width={700} height={460} className="h-56 w-full object-cover" loading="lazy"/><div className="p-5"><p className="text-sm font-bold text-amber-700">{item.category} · {item.location}</p><h3 className="mt-1 text-xl font-black">{item.title}</h3><p className="mt-2 text-slate-600">{item.description}</p></div></article>}
export function TestimonialCard({item}:{item:Testimonial}){return <article className="rounded-3xl bg-white p-6 shadow-sm"><div className="flex text-amber-500">{Array.from({length:item.rating}).map((_,i)=><Star key={i} size={18} fill="currentColor"/>)}</div><p className="mt-4 text-slate-700">“{item.text}”</p><strong className="mt-4 block">{item.name}</strong><span className="text-sm text-slate-500">{item.city}</span></article>}
