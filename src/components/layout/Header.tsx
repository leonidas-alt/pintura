'use client';
import { Menu, X } from 'lucide-react';import Link from 'next/link';import { useState } from 'react';import { professional } from '@/data/professional';import { Button } from '@/components/ui/Button';
const links=[['/','Início'],['/servicos','Serviços'],['/portfolio','Portfólio'],['/sobre','Sobre'],['/contato','Contato']];
export function Header(){const[open,setOpen]=useState(false);return <header className="sticky top-0 z-40 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="container flex h-20 items-center justify-between"><Link href="/" className="font-black text-slate-900">{professional.name}</Link><nav className="hidden gap-6 md:flex">{links.map(([h,l])=><Link className="focus-ring rounded text-sm font-semibold text-slate-700 hover:text-slate-950" key={h} href={h}>{l}</Link>)}</nav><div className="hidden md:block"><Button href="/orcamento">Pedir orçamento</Button></div><button aria-label="Abrir menu" onClick={()=>setOpen(!open)} className="focus-ring md:hidden">{open?<X/>:<Menu/>}</button></div>{open&&<div className="border-t bg-white p-4 md:hidden"><nav className="grid gap-3">{links.map(([h,l])=><Link onClick={()=>setOpen(false)} key={h} href={h} className="rounded-lg p-3 font-semibold">{l}</Link>)}<Button href="/orcamento">Pedir orçamento</Button></nav></div>}</header>}
