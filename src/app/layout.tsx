import type { Metadata } from 'next';import './globals.css';import { Header } from '@/components/layout/Header';import { Footer } from '@/components/layout/Footer';import { WhatsAppButton } from '@/components/layout/WhatsAppButton';import { professional } from '@/data/professional';
export const metadata: Metadata = { title: { default: `${professional.name} | Pintura profissional`, template: `%s | ${professional.name}` }, description: 'Site profissional para orçamento de pintura residencial, comercial, grafiato, texturas e massa corrida.', openGraph: { title: `${professional.name} | Pintura profissional`, description: professional.slogan, type: 'website', locale: 'pt_BR' } };
export default function RootLayout({children}:{children:React.ReactNode}){return <html lang="pt-BR"><body><Header/><main>{children}</main><Footer/><WhatsAppButton/></body></html>}
