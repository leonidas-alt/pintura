import type { Metadata } from 'next';import { PortfolioCard } from '@/components/sections/Cards';import { SectionTitle } from '@/components/ui/SectionTitle';import { portfolio } from '@/data/content';
export const metadata:Metadata={title:'Portfólio',description:'Veja exemplos de trabalhos realizados em pintura e acabamento.'};export default function Page(){return <section className="py-16"><div className="container"><SectionTitle eyebrow="Portfólio" title="Trabalhos realizados"/><div className="grid gap-5 md:grid-cols-3">{portfolio.map(p=><PortfolioCard key={p.title} item={p}/>)}</div></div></section>}
