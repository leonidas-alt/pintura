import type { Metadata } from 'next';import { notFound } from 'next/navigation';import Image from 'next/image';import { Button } from '@/components/ui/Button';import { services } from '@/data/services';
export function generateStaticParams(){return services.map(s=>({slug:s.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const service=services.find(s=>s.slug===slug);return {title:service?.title??'Serviço',description:service?.summary}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const service=services.find(s=>s.slug===slug);if(!service)notFound();return <section className="py-16"><div className="container grid gap-8 md:grid-cols-2"><Image src={service.image} alt={service.title} width={700} height={460} className="rounded-3xl bg-white shadow"/><div><p className="font-bold text-amber-700">Serviço</p><h1 className="mt-2 text-4xl font-black">{service.title}</h1><p className="mt-4 text-lg text-slate-600">{service.description}</p><ul className="mt-6 grid gap-3">{service.benefits.map(b=><li key={b} className="rounded-xl bg-white p-3 font-semibold">✓ {b}</li>)}</ul><Button href="/orcamento" className="mt-8">Solicitar orçamento</Button></div></div></section>}
