import type { Metadata } from 'next';import { professional } from '@/data/professional';import { SectionTitle } from '@/components/ui/SectionTitle';
export const metadata:Metadata={title:'Sobre',description:'Conheça o profissional de pintura.'};export default function Page(){return <section className="py-16"><div className="container"><SectionTitle eyebrow="Sobre" title={professional.name} description={`Profissional com ${professional.experienceYears} de experiência, atuando em ${professional.cityRegion}.`}/><p className="max-w-3xl text-lg text-slate-700">Trabalho focado em comunicação clara, preparo correto das superfícies, organização durante a execução e acabamento adequado para cada ambiente.</p></div></section>}
