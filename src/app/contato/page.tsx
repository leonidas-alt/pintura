import type { Metadata } from 'next';import { professional } from '@/data/professional';import { Button } from '@/components/ui/Button';import { SectionTitle } from '@/components/ui/SectionTitle';import { buildWhatsAppUrl } from '@/lib/whatsapp';
export const metadata:Metadata={title:'Contato',description:'Fale pelo WhatsApp, e-mail ou Instagram.'};export default function Page(){const url=buildWhatsAppUrl(professional.whatsappNumber,'Olá! Gostaria de falar sobre um serviço de pintura.')??'/orcamento';return <section className="py-16"><div className="container"><SectionTitle eyebrow="Contato" title="Fale com o profissional"/><div className="rounded-3xl bg-white p-6 shadow-sm"><p>WhatsApp: {professional.phoneLabel}</p><p>E-mail: {professional.email}</p><p>Instagram: {professional.instagram}</p><Button href={url} className="mt-6">Chamar no WhatsApp</Button></div></div></section>}
